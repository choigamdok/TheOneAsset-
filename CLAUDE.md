# 이 저장소에서 작업할 때

**The One Financial Suite** — 재무설계 마스터의 변형본. `index.html` 한 장짜리
정적 페이지다. 빌드 도구도 의존 패키지도 없다.

## ⚠️ 먼저 알아야 할 것

**1. 저장소 이름 끝에 하이픈이 붙어 있다**

```
TheOneAsset-
```

오타로 만들어진 이름으로 보인다. 주소를 적을 때 하이픈을 빠뜨리면 저장소를 못 찾는다.
이름을 바꾸면 기존 링크와 Vercel 연동이 끊기므로, **사용자 요청 없이 바꾸지 않는다.**

**2. 같은 계산기가 저장소 세 곳에 있다**

| 저장소 | 상태 | 마지막 작업 |
|---|---|---|
| `gumshowmom` | ✅ 가장 최신·완전판 (리포트 6개 분리) | 2026-02-11 |
| `financial-master` | ⚠️ 구버전. 복리 리포트 파일이 빠져 링크가 깨져 있다 | 2026-01-30 |
| **`TheOneAsset-`** (여기) | ⚠️ 별개 변형 | 2026-01-30 |

여기는 다른 둘과 구조가 다르다. **리포트 파일이 없고 `index.html` 한 장뿐이다.**
제목도 다르다("The One Financial Suite — Premium Asset Management" / 다른 둘은
"재무설계 마스터 — 통합 재무 계산기").

즉 **여기서 고친 내용은 다른 두 저장소에 반영되지 않는다.** 반대도 마찬가지다.
계산기를 고쳐달라는 요청을 받으면 **어느 주소의 페이지인지 사용자에게 먼저 확인한다.**
세 저장소가 각각 별도의 Vercel 프로젝트로 배포돼 있다(여기는 `the-one-asset`).

정리(하나로 합치기)는 사용자가 명시적으로 요청할 때만 한다. 임의로 다른 저장소를
지우거나 옮기지 않는다.

## 파일

```
index.html   페이지 전체 (CSS·JS 인라인)
README.md    한 줄짜리 메모
```

## 반드시 지킬 것

**1. 계산식을 임의로 바꾸지 않는다** — 고객 상담에 쓰는 수치가 나온다. 수익률·세율 같은
   기본값이나 계산 로직을 "더 합리적으로 보이게" 고치지 않는다. 변경은 사용자 확인을 받는다.

**2. 시작할 때 `git pull`, 끝나면 push** — 맥에어·맥미니 두 대에서 작업한다.
   충돌이 나면 임의로 진행하지 말고 사용자에게 알린다.

**3. 수동 배포 금지** — `vercel --prod` 같은 CLI 직접 배포를 쓰지 않는다. push만 한다.

**4. main push 전에 승인받는다** — 올리면 바로 공개된다.

## 확인

```bash
open index.html
```

숫자를 만지는 앱이므로 **경계값을 넣어본다** — 0, 빈칸, 아주 큰 값. 결과가 `NaN`
이나 `Infinity` 로 새지 않는지 본다. 모바일 폭(390px)에서도 본다.
